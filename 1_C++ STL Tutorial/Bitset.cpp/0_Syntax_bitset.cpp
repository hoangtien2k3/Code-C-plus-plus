
/*  C++ bitset Functions:

    Bitset là một mảng các phần tử bool, nhưng được tối ưu hóa để phân bổ không gian, 
    nói chung, mỗi phần tử chỉ chiếm một bit (mà trên hầu hết các hệ thống, 
    ít hơn tám lần so với loại phần tử nhỏ nhất là char).

*/